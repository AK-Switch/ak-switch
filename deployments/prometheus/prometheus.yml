# Prometheus configuration for AK Switch
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'akswitch'
    static_configs:
      - targets: ['akswitch:3000']
    metrics_path: /metrics
    # Relabel to add a human-readable instance label
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: 'akswitch'
