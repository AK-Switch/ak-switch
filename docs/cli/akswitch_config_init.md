## akswitch config init

Initialize default config.toml

### Synopsis

Create a default configuration file at the XDG config directory
(or a custom path via --path).

If the file already exists, the command refuses to overwrite it.

```
akswitch config init [flags]
```

### Options

```
  -h, --help          help for init
  -p, --path string   Output path for config.toml (default: XDG config directory)
```

### SEE ALSO

* [akswitch config](akswitch_config.md)	 - Manage configuration

